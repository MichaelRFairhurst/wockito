every ReturnValueStubber{T} is:

	with T[] returns = [];
	with Num = 0;

	ReturnValueStubber{T} -- thenReturn(T) {
		returns.push(T);
		return this;
	}

	T -- get() {
		var T = returns[Num];
		Num += 1;
		return T;
	}
